# name: Fixed Workflow

# on: [workflow_dispatch]

# env:
#   GOTOOLCHAIN: local
#   GOFLAGS: "-mod=readonly"

# jobs:
#   stable-build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: '1.24.2'

#       - name: Fixed Cache Step
#         uses: actions/cache@v4
#         with:
#           # FIX: We only cache the SOURCE modules, not the BUILD binaries
#           path: ~/go/pkg/mod
#           key: ${{ runner.os }}-go-stable-v1-${{ hashFiles('**/go.sum') }}

#       - name: This will now pass
#         run: go list -json -deps ./...